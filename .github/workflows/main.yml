name: Build Oraxen v1.214.0
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Oraxen v1.214.0
        uses: actions/checkout@v4
        with:
          repository: oraxen/oraxen
          ref: v1.214.0

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'

      - name: Build with Gradle (with retries)
        run: |
          for i in 1 2 3 4 5; do
            echo "Attempt $i..."
            ./gradlew shadowJar --no-daemon && break
            echo "Build failed, waiting before retry..."
            sleep 60
          done

      - name: Upload plugin jar
        uses: actions/upload-artifact@v4
        with:
          name: oraxen-v1.214.0
          path: build/libs/*.jar
